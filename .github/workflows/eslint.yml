on:
  pull_request:
    types: [opened, synchronize]

jobs:
  linter:
    runs-on: ubuntu-latest
    container: "node:alpine"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Build back-end and front-end
        run: npm ci --prefix=back-end && npm ci --prefix=front-end && echo "--\n" && npm -g ls --depth=0 && echo "--\n" && npm ls --depth=0
      - name: Run ESLint
        run: npx eslint .